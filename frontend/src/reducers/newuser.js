let initialState = {
    name: '',
    age: 0,
    title: '',
    project: ''
};
export default function newuser(state=initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}